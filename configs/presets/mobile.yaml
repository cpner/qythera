preset: mobile
description: "Qythera Mobile configuration"
model:
  name: vaelon-7b
  quantization: int4
  max_tokens: 4096
inference:
  backend: cpu
  batch_size: 1
  timeout: 120
memory:
  enabled: true
  vector_store: local
safety:
  toxicity: true
  jailbreak: true
  pii: false
logging:
  level: INFO
  wandb: false
