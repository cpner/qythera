preset: edge
description: "Qythera Edge configuration"
model:
  name: vaelon-7b
  quantization: int4
  max_tokens: 4096
inference:
  backend: cpu
  batch_size: 8
  timeout: 120
memory:
  enabled: true
  vector_store: qdrant
safety:
  toxicity: true
  jailbreak: true
  pii: false
logging:
  level: INFO
  wandb: false
