preset: minimal
description: "Qythera Minimal configuration"
model:
  name: vaelon-7b
  quantization: none
  max_tokens: 512
inference:
  backend: gpu
  batch_size: 1
  timeout: 30
memory:
  enabled: false
  vector_store: local
safety:
  toxicity: true
  jailbreak: true
  pii: false
logging:
  level: WARNING
  wandb: false
