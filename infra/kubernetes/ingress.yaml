apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qythera-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: qythera.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qythera-service
                port:
                  number: 80
